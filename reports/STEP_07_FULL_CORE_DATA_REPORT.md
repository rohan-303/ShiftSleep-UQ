# ShiftSleep-UQ Step 7 Full Core Data Report

## 1. Status

BLOCKED

Step 7 stopped during repository preflight. No full-core acquisition, preprocessing, cohort construction, or dataset freeze was executed.

## 2. Core Data Gate

BLOCKED

The required frozen-contract preflight is inconsistent: the committed data contract contains unsupported ISRUC aliases while the Step 6.1 repair report states that those aliases were removed.

## 3. Final Benchmark Gate

NO

SHHS1 remains outside the authorized accessible core because authorized raw/XML access and per-record validation remain pending.

## 4. Repository State

* repository: `C:\Users\rohan\ShiftSleep-UQ`
* branch: `main`
* HEAD: `de22b495f6576bb28c4aad46106a9963e950a929`
* HEAD message: `fix: repair ISRUC annotation preprocessing`
* Step 6.1 commit verified: `de22b495f6576bb28c4aad46106a9963e950a929`
* Step 6.1 report is committed at `reports/STEP_06_1_PREPROCESSING_REPAIR_REPORT.md`
* preflight working tree before this blocked report: clean
* this Step 7 report is currently uncommitted
* GitHub push: not performed

## 5. Frozen Contracts

Data contract:

* version: `1.1.0`
* status: `CORE_PREPROCESSING_FROZEN`
* `final_benchmark_frozen: false`
* current SHA256: `a9b05b22697fe8ba5cb3f03a6e35cca2fcbdb55971d6e565a7dc9b36ab68a0f9`

Preprocessing:

* version: `0.1.0`
* SHA256: `70cd20ccee62b14a23e5996c9f76b6a708a4e69bde03f36bdf4ee8dffa9ef794`

The status fields themselves are consistent with Step 7 authorization. The semantic mapping representation is not consistent with the Step 6.1 repair claim.

## 6. Expected Population

Not created. Step 7 requires this manifest to be created and frozen before acquisition, but preflight stopped before population definition.

Required future expectations:

* Sleep-EDF SC: 78 participants and 153 recordings
* ISRUC-S1: 100 participants and 100 recordings

## 7. Acquisition Results

Not executed.

## 8. Provider Integrity

Not executed. Existing Step 6.1 evidence for I001/I002 remains historical smoke evidence only and was not expanded during this blocked step.

## 9. ISRUC Provider Viability

NOT DECIDED

A Step 7 full-core viability classification requires the authorized 100-subject acquisition audit. No classification is assigned because acquisition did not begin.

## 10. Raw Schema Results

Not executed for the full core.

## 11. Preprocessing Results

Not executed for the full core.

No new raw or processed signal artifacts were acquired or generated by Step 7.

## 12. Subject-Level Results

Not executed.

## 13. Structural Exclusions

Not generated.

## 14. Epoch Accounting

Not executed for the full core. Existing Step 6.1 smoke accounting remains unchanged and is not a Step 7 result.

## 15. Stage Distribution

Not generated. No stage-based inclusion or exclusion decision was made.

## 16. Annotation Exclusions

Not generated for the full core.

## 17. Signal QC

Not executed for the full core.

## 18. Unit-Scale Audit

Not executed for the full core.

## 19. Duration / Alignment Audit

Not executed for the full core.

## 20. Duplicate Audit

Not executed for the full core.

## 21. Domain Shift Inventory

Not modified. Existing domain-shift documentation remains unchanged.

## 22. Processed Output Integrity

Not executed for the full core. Existing Step 6.1 smoke outputs were not changed by Step 7.

## 23. Determinism Audit

Not executed for the full core.

## 24. Frozen Accessible Cohort

Not frozen.

No Step 7 subjects, recordings, or valid-epoch totals are included because the preflight gate failed before expected-population creation.

## 25. Cohort Inclusion Policy

Not operationalized. The policy supplied for Step 7 remains the intended policy, but no recording-level decisions were made.

## 26. Cohort Config

Not created. `configs/core_cohort_v1.yaml` must not be created until the preflight inconsistency is resolved and the full accessible core is audited.

## 27. Subject Manifest

Not created.

## 28. Recording Manifest

Not created.

## 29. Storage

No Step 7 acquisition or processing storage was added.

## 30. Leakage Protections

Confirmed for this blocked step:

* no split creation;
* no stage-based inclusion;
* no model-based QC;
* no target-derived normalization;
* no cross-subject mixing;
* no label-derived Wake trimming;
* no scorer cherry-picking;
* no model implementation or training.

## 31. Tests Added

None. Step 7 implementation was not started because the frozen-contract preflight failed.

## 32. Validation

Preflight validation completed:

* repository identity: passed;
* branch verification: `main`;
* Step 6.1 commit lookup: passed;
* Step 6.1 report existence: passed;
* required config/report/document files: present;
* `pytest -q`: **39 passed in 1.92s**;
* current contract SHA256 equals recorded `reports/preprocessing_hashes.txt`: passed;
* working-tree status before creating this report: clean;
* blocking semantic inconsistency: confirmed.

Blocking evidence:

```text
configs/data_contract_v1.yaml:12:  N1: ["Sleep stage 1", "Sleep stage N1", "Stage 1", "N1"]
configs/data_contract_v1.yaml:13:  N2: ["Sleep stage 2", "Sleep stage N2", "Stage 2", "N2"]
configs/data_contract_v1.yaml:14:  N3: ["Sleep stage 3", "Sleep stage 4", "Sleep stage N3", "Stage 3", "Stage 4", "N3"]
```

The same committed file has SHA256 `a9b05b22697fe8ba5cb3f03a6e35cca2fcbdb55971d6e565a7dc9b36ab68a0f9`, matching the recorded repaired hash. Therefore the inconsistency is in the committed Step 6.1 artifact itself, not a local uncommitted edit.

## 33. Files Created

* `reports/STEP_07_FULL_CORE_DATA_REPORT.md` — blocked-step report; currently uncommitted

## 34. Files Modified

None before creation of this report. No scientific contract, preprocessing code, source data, or Step 6.1 artifacts were modified.

## 35. Explicitly Not Done

Confirmed:

* no model;
* no training;
* no normalization;
* no final splits;
* no ML metrics;
* no calibration;
* no SHHS bypass;
* no full-core acquisition;
* no full-core preprocessing;
* no expected population manifest;
* no subject manifest;
* no recording manifest;
* no cohort config;
* no raw signals committed;
* no processed signals committed;
* no push.

## 36. Remaining Accessible-Core Issues

The Step 6.1 contract artifact must be reconciled before Step 7 can proceed. Specifically, the project must determine whether `Sleep stage 1/2/3` and `Stage 1/2/3` are intentionally retained as explicit cross-dataset aliases or must be removed as stated by the Step 6.1 report. This cannot be guessed during Step 7.

Any resolution must be evidence-backed, recorded as a scientific decision if it changes the contract representation, followed by a fresh hash update, tests, and Step 6.1 report correction if necessary.

## 37. Remaining Final-Benchmark Issues

SHHS1 remains separate and blocked by:

* authorized raw/XML access;
* exact required-channel validation;
* per-record rate, unit, identity, and annotation validation;
* final benchmark freeze.

## 38. Recommended Step 8

Not applicable. Step 8 must not be considered until Step 7 completes with a non-blocked accessible-core gate and the accessible cohort is frozen.

## 39. Git Status / Diff Summary

Step 7 was stopped before acquisition and data mutation. The only current uncommitted file is this blocked report:

`reports/STEP_07_FULL_CORE_DATA_REPORT.md`

No raw or processed signal artifacts were added. No GitHub push was performed.

The next permitted action is to resolve the Step 6.1 contract/report inconsistency and rerun the Step 7 preflight. Full-core acquisition must not begin before that gate passes.
